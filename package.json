{
  "name": "fsais-root",
  "version": "1.0.0",
  "description": "Faculty & Student Academic Intelligence System (FSAIS) - Sri Eshwar College of Engineering",
  "scripts": {
    "install-all": "npm install --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build-frontend": "npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
